# 電子麻酔記録システム - 起動方法

## 初回セットアップ（一度だけ実行）

1. Node.jsをインストール: https://nodejs.org/
2. アプリケーションフォルダで以下を実行:
   ```
   npm install
   ```

## 起動方法

### Windows
- `start.bat` をダブルクリック

### Mac/Linux
- `start.sh` をダブルクリック
- または、ターミナルで `./start.sh` を実行

### 手動起動
```
npm start
```

## 実行可能ファイルの作成（オプション）

Windows用実行可能ファイルを作成する場合:
```
npm run build-win
```

作成された実行可能ファイルは `dist/` フォルダに保存されます。

## UI改善点

✅ **実装済み機能:**
- Panel 1の5セクションがコラプシブル（折りたたみ可能）
- 最新バイタルサインの枠付き数値表示
- Panel 2のタイムラインが絶対時刻表示
- Panel 3のチャート凡例が右端に配置
- バイタルサインの記号がチャートに表示
- Panel 3もコラプシブル対応

## 設定ファイル

CSV形式の設定ファイルは `config/` フォルダにあります:
- `drugs.csv` - 薬剤設定
- `remarks.csv` - リマークス設定
- `ivfluids.csv` - 輸液設定
- `vitalsigns.csv` - バイタルサイン設定
