# VitalDB API Integration Guide

## 概要
3パネル麻酔記録ソフトウェアにVitalDB API連携機能を実装しました。リアルタイムバイタルサインデータの取得、認証管理、オフライン時のフォールバック機能を提供します。

## 新機能

### 1. VitalDB API認証
- OAuth2 bearerトークン認証（1時間有効）
- 自動トークン更新機能
- 認証状態の可視化

### 2. リアルタイムデータ取得
- `/api/receive`エンドポイントからのバイタルサインデータ取得
- 既存のChart.js可視化システムとの統合
- VitalDB JSON形式からChart.jsデータ形式への自動変換

### 3. 設定UI
- データソース選択（模擬データ / VitalDB API）
- VitalDB認証情報入力
- VRコード（9桁識別子）設定
- 接続状態表示

### 4. エラーハンドリング
- ネットワークエラー時の自動フォールバック
- 認証失敗時の再試行機能
- オフライン時の模擬データ継続

## 使用方法

### 1. 初期設定
1. アプリケーションを起動
2. 第一パネルの「設定」セクションを展開
3. 「データソース」で「VitalDB API」を選択
4. VitalDB認証情報を入力：
   - ユーザー名
   - パスワード
   - VRコード（9桁の数字）
5. 「VitalDB接続」ボタンをクリック

### 2. データ取得開始
1. 第三パネルの「取得開始」ボタンをクリック
2. VitalDB APIからリアルタイムデータ取得開始
3. API接続失敗時は自動的に模擬データにフォールバック

### 3. 設定変更
- バイタル更新間隔：30秒、1分、2分から選択可能
- 時間枠設定：4時間、8時間、16時間から選択可能
- 設定変更は即座にチャートに反映

## 技術仕様

### API エンドポイント
- **認証**: `POST https://vitaldb.net/api/login`
- **データ取得**: `POST https://vitaldb.net/api/receive`

### データマッピング
| VitalDB Track | アプリケーション |
|---------------|------------------|
| HR            | 心拍数           |
| SpO2          | 酸素飽和度       |
| SBP           | 収縮期血圧       |
| DBP           | 拡張期血圧       |
| TEMP          | 体温             |
| RR            | 呼吸数           |
| ETCO2         | 呼気終末CO2      |

### 依存関係
- **axios**: HTTP クライアント（v1.6.0）
- **chart.js**: チャート可視化（既存）
- **electron**: デスクトップアプリケーション（既存）

## ファイル変更

### package.json
- axios依存関係を追加

### index.html
- VitalDB設定UIを追加
- データソース選択機能

### renderer.js
- VitalDB API認証メソッド
- データ取得・変換メソッド
- エラーハンドリング機能
- 設定イベントリスナー

### styles.css
- VitalDB設定UIスタイル
- 状態インジケータースタイル

## セキュリティ考慮事項

### 認証情報の取り扱い
- パスワードはメモリ内でのみ保持
- アクセストークンは1時間で自動期限切れ
- 認証失敗時の適切なエラーハンドリング

### ネットワーク通信
- HTTPS通信のみ使用
- OAuth2 Bearer認証
- 適切なエラーレスポンス処理

## トラブルシューティング

### 接続エラー
1. ネットワーク接続を確認
2. VitalDB認証情報を再確認
3. VRコードの形式確認（9桁の数字）

### データ取得エラー
- 自動的に模擬データにフォールバック
- コンソールログでエラー詳細確認
- 認証トークンの自動更新

### UI表示問題
- ブラウザの開発者ツールでエラー確認
- アプリケーション再起動

## 今後の拡張可能性

### データ送信機能
- `/api/send`エンドポイントでのデータアップロード
- イベント記録のVitalDBへの送信

### 高度な設定
- カスタムエンドポイント設定
- データ取得間隔の詳細設定
- 複数VRコード対応

### 分析機能
- データ品質監視
- 接続統計情報
- パフォーマンス最適化

## サポート
VitalDB API仕様の詳細については、公式ドキュメント（https://vitaldb.net/docs/）を参照してください。
